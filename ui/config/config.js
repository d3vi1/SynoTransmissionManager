// TransmissionManager UI configuration
// Loaded by DSM when the application initializes

Ext.ns('SYNO.SDS.TransmissionManager');

SYNO.SDS.TransmissionManager.Config = {
    POLL_INTERVAL: 2000,    // Torrent list refresh interval (ms)
    API_VERSION: 1,
    APP_NAME: 'SYNO.SDS.TransmissionManager'
};
